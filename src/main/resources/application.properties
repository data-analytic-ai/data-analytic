
server.port=${SERVER_PORT:8081}

jwt.secret=${JWT_SECRET:e9aba6bc57503bb4356e4e6eba9027a5}

# Job Data Source (Primary)
db.job.repo.jdbc-url=${JOB_REPO_JDBC_URL:jdbc:postgresql://localhost:5432/spring-batch-metadata}
db.job.repo.username=${JOB_REPO_USERNAME:postgres}
db.job.repo.password=${JOB_REPO_PASSWORD:qwerty}
db.job.repo.driver-class-name=org.postgresql.Driver


logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.jdbc.datasource = DEBUG
logging.level.org.springframework.boot.autoconfigure.jdbc = DEBUG
logging.level.org.postgresql=DEBUG

spring.batch.jdbc.initialize-schema=always

batch.db.initialize-schema=ALWAYS
query.bridge.database.url=http://localhost:8081/query/bridge/database

spring.jpa.open-in-view=false

# --- MongoDB ---
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/dataanalitycs}